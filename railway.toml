[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python startup.py && gunicorn app:app --bind 0.0.0.0:$PORT --workers 1 --timeout 300"
healthcheckPath = "/api/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
